// Use this config file for global configuration

module.exports = {
  // Global server config
  server: {
    port: 8080
  },

  // Electron app config
  electron: {
    height: 600,
    width: 800
  }
};
